import 'package:flutter/material.dart';

class DistributionManagementViewBody extends StatelessWidget {
  const DistributionManagementViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}